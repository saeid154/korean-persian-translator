{
  "name": "korean-persian-translator-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend proxy that keeps the OpenAI API key off the client and serves the static web app.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "multer": "^2.4.0"
  }
}
